# 博客前端优化说明

本文件记录博客（Hexo + Hingle 主题）当前的前端状态与维护方法。

> 2025 更新：应博主要求，**整体样式已恢复为 Hingle 原版效果**（含随机背景图、原版配色与布局），只在其上叠加了少量不改变原版观感的功能与细节。

---

## 一、当前保留的优化

### 1. 代码块（Prism 高亮 + 复制按钮 + 语言标签）
- 高亮由 Hexo 内置 Prism 在**构建时**完成（根目录 `_config.yml` 的 `syntax_highlighter: prismjs`），页面无需加载 prism.js。
- 每个代码块自动显示：**语言标签**（左上角，很淡）+ **复制按钮**（右上角，鼠标悬停才出现，保持原版简洁）。
- 无语言标识的 `<pre><code>` 自动补 `language-none`，样式统一。
- 配色沿用原版 Prism 明亮主题，夜间模式自动适配。

### 2. 图片加速（无感知）
- **原生懒加载**：`marked.lazyload: true`，构建时为图片加 `loading="lazy"`。
- **jsDelivr CDN**：主题 `img_cdn` 开启时，站内图片自动走 `cdn.jsdelivr.net`，**加载失败自动回退原链接**，不会裂图。不需要可设 `enable: false`。

### 3. 小羊桌宠（新增 ⭐）
- 右下角的小羊，进入博客自动出现；**鼠标左键按住可拖动**，位置会被记住。
- **单击**：播放/暂停学习音乐，并弹出对话气泡（显示正在播放的歌名或随机卖萌语）。
- **双击**：切换到下一首。
- 播放时小羊右下角有迷你均衡器动效，还有呼吸、眨眼、耳朵摇动等小动画。
- 位置、音量、曲目都会保存在浏览器本地。

### 4. 学习背景音乐
- 桌宠播放的纯音乐歌单在主题 `_config.yml` 的 `music.list` 中配置，默认 4 首 Pixabay 免版权曲子（已实测可外链）。
- 浏览器会拦截自动播放，所以需要**点击小羊**才开始播放，这是浏览器策略。

### 5. 其他
- 随机背景图保留（`themes/hingle/_config.yml` 的 `background`）。
- 头部 meta 描述、预连接等细微优化（不影响外观）。

---

## 二、日常维护

### 换背景音乐
1. 打开 https://pixabay.com/music/ 选一首纯音乐，复制下载直链（`https://cdn.pixabay.com/download/audio/...`）。
2. 编辑 `themes/hingle/_config.yml` 的 `music.list`。
3. 重新生成部署即可。

### 关掉某个功能
| 功能 | 开关位置（themes/hingle/_config.yml） |
| --- | --- |
| 桌宠 | `pet.enable: false` |
| 音乐 | `music.enable: false`（桌宠还在，只不再放歌） |
| 图片 CDN | `img_cdn.enable: false` |
| 随机背景 | 注释掉 `background` 一行 |
| 夜间模式 | `config.night: false` |

### 首页细节优化
`hingle.css` 末尾有一小段「首页细节优化」（标题字距、摘要颜色、社交图标悬停等）。
如果连这些也不想要，**删除该段即可 100% 还原最初效果**。

### 发布流程
```bash
hexo clean && hexo generate && hexo deploy
```

---

## 三、图片加速可选方案

- **jsDelivr（当前已用）**：零成本，`img_cdn.repo` 写对仓库即可。新图片首次访问可能需刷新一次（CDN 回源缓存）。
- **图床（SM.MS / 路过图床）**：适合单张图，上传后复制外链替换 Markdown 里的图片链接；有失效风险，重要图片留本地备份。
- **独立图片仓库 + jsDelivr**：把图片仓库的链接直接写在文章里，主题 `img_cdn.repo` 改为该仓库。
