---
title: 高可用与负载均衡-1
date: 2026-07-24 00:18:38
tags: [Linux, CentOS, 运维, 高可用与负载均衡]
categories: 大二集训
---

# 7.22 课程笔记：高可用与负载均衡-1

> 7/22 am 

本节进入了新的一个小专题，与服务器架构相关，老师给这节课取名为"高可用与负载均衡"。我们可以新开虚拟机的环境，让实验环境是干净清晰的。

---

## 一、服务器架构与核心概念

### 1. 高可用（HA）与负载均衡（LB）

> 我们先了解一下这几个关键词吧

- **高可用**：核心目标是**消除单点故障**，当系统某个组件（如服务器、调度器）发生故障时，能自动切换到备用组件，保证服务不中断。最典型的实现是双机热备，分为主备模式（仅主节点工作，备节点待命）和双活模式（双节点同时处理请求，需搭配分布式文件系统如 Ceph/GFS，避免存储脑裂）。生产环境中还需配合电源控制器、直连心跳线（独立于业务网络的网卡直连，规避交换机故障风险）进一步提升可靠性。

- **负载均衡**：通过**将客户端流量合理分发到多台后端服务器**，解决单台服务器性能瓶颈，提升系统整体处理能力。负载均衡器本身也是单点，因此通常与高可用方案结合部署。常见调度算法包括轮询、加权轮询、最少连接、源地址哈希等。

- **CDN（内容分发网络）**：现代网站的标配组件，通过全球边缘节点缓存静态内容，将用户请求就近返回，既加速访问又减轻源站压力。其工作原理类似分级缓存：用户请求先查本地 CDN 节点，未命中则查询邻近节点，最终才回源站。需注意 CDN 失效时可能引发后端流量雪崩，需配合降级、限流等手段兜底。中小厂商通常采购云厂商 CDN 服务，自建 CDN 成本过高，仅适合超大型互联网企业。

### 2. 常见服务器架构对比

- **CS 架构（客户端-服务器）**：适合低并发场景，早期应用较多，客户端需安装专用软件。
- **BS 架构（浏览器-服务器）**：当前主流架构，用户通过浏览器以 HTTP 协议访问 Web 服务器，后端对接数据库，更适配现代动态网页需求。云端环境通常直接提供封装好的高可用与负载均衡能力，自建架构则需手动实现相关技术栈。

### 3. 负载均衡技术分层

- **四层负载均衡**：工作在传输层（OSI 第四层），代表方案为 LVS，基于 IP+端口转发流量，性能极高，但无法识别应用层内容。
- **七层负载均衡**：工作在应用层（OSI 第七层），代表方案为 HAProxy、Nginx，可基于 HTTP URL、Header 等内容做精细化调度（如动静分离、会话保持），对后端服务透明，无需配置 VIP。

![](0.png)

> 📌 **补充知识点**：四层和七层不是替代关系，而是分层协作。生产环境常见架构：**LVS（四层入口）→ HAProxy（七层分发）→ 后端集群**，兼顾性能和灵活性。

---

## 二、LVS-DR 模式负载均衡

### （1）ipvsadm 工具与基础命令

1. `ipvsadm` 是 Linux 下用于管理 IPVS（IP Virtual Server，IP 虚拟服务器）的命令行工具，主要用于构建 LVS（Linux Virtual Server）负载均衡集群。它允许管理员配置和管理虚拟服务、真实服务器（后端节点）以及负载均衡策略。

2. 一些相关命令

   1）基础的查看命令

   ```bash
   ipvsadm -Ln           # 查看规则
   ipvsadm -Ln --stats   # 查看流量统计
   ipvsadm -Ln --rate    # 查看连接速率
   ```

   2）添加 VIP 虚拟服务

   ```bash
   ipvsadm -A -t VIP:端口 -s 算法
   # -A：新建虚拟服务  -t：TCP协议  -s：指定调度算法
   ```

   3）添加后端 RS 节点

   ```bash
   ipvsadm -a -t VIP:端口 -r RIP:端口 -g/-m/-i
   # -a：增加真实服务器  -r：后端服务器地址
   # 转发模式三选一：
   # -g DR 直接路由（本次实验，性能最高）
   # -m NAT 模式
   # -i IP 隧道 TUN
   ```

   4）持久化保存

   ```bash
   ipvsadm-save > /etc/sysconfig/ipvsadm
   ipvsadm-restore < /etc/sysconfig/ipvsadm
   ```

3. LVS 有三种转发工作模式：

   - **DR（Direct Routing）模式** `-g`：性能最优，调度器与后端服务器在同一网段，通过修改数据包的 MAC 地址实现转发，后端服务器需绑定 VIP。
   - **NAT（Network Address Translation）模式** `-m`：性能较差，调度器与后端服务器不在同一网段，调度器需进行地址转换（SNAT），后端服务器需配置网关指向调度器。
   - **TUN（IP Tunneling，IP 隧道）模式** `-i`：适用于跨网段场景（公网），通过 IP 隧道协议封装数据包，性能介于 DR 和 NAT 之间。

   > 📌 **补充知识点：第四种模式 — FULLNAT**
   > 除了 DR/NAT/TUN 三种经典模式外，还有 **FULLNAT 模式**（由阿里开源贡献）。它将请求和响应的源 IP、目标 IP 都做转换，使 RS 看不到客户端真实 IP，但部署更灵活（RS 可跨 VLAN），常用于大型云厂商内部。

4. 常用的调度算法有四个：
   - `rr` 轮询：请求依次轮流分配，不考虑负载
   - `wrr` 加权轮询：根据权重分配请求
   - `lc` 最少连接；`wlc` 加权最少连接
   - `sh` 源地址哈希，实现会话保持

### （2）具体的实验

这次实验需要四台机子，他们的功能如下：

| 主机名 | IP (RIP) | VIP | 角色 | 备注 |
|--------|----------|-----|------|------|
| server1 | 192.168.63.131 | 192.168.63.100 | 调度器（VS） | 负载调度器，接收客户端请求，分发流量 |
| server2 | 192.168.63.128 | 192.168.63.100 | 真实服务器（RS） | 真实业务服务器，处理请求并返回响应 |
| server3 | 192.168.63.129 | 192.168.63.100 | 真实服务器（RS） | 真实业务服务器，处理请求并返回响应 |
| server4 | 192.168.63.130 | — | 测试机 | — |

> Ps: vip 长得和会员 VIP 用户的 VIP 好像（VIP：虚拟 IP，客户端访问的入口）

**1. 在实验开始之前，我们需要了解一下这是在干什么。**

我们想要搭建四层的负载均衡集群，让调度器 server1 分发流量给后端 server2、server3。由于 DR 模式性能最高，我们选用 DR 模式。

好的，实验开始了。

我们先给所有主机配置静态 IP（防止 DHCP 自动分配改变 IP，LVS 规则和关系失控），这样也方便 PC 互相 ssh。

> Ps: 我之前实验时，把网关配置成 10 网段的了（如 10.0.0.132），但是老师用的是 192.168. 的，想改回去。这要求我们关闭虚拟机，打开"编辑"里面的"虚拟网络编辑器"，使它恢复默认。

打开 `/etc/sysconfig/network-scripts/ifcfg-ens33`（网卡配置文件），添加 `IPADDR=192.168.63.131`、`NETMASK=255.255.255.0`、`GATEWAY=192.168.63.2`、`DNS1=192.168.63.2`。保存退出后，重启网络。之后，重新配置主机名。

![](1.png)

**2. 在调度器 server1 上配置 LVS**

![](2.png)

我们安装好 ipvsadm 后，找到物理网卡（ens33），绑定 VIP（对外的接口 /24）；

清空旧规则（虽然我没搞这个，命令是 `ipvsadm -C`）；

创建虚拟服务，用 rr 轮询算法（rr 轮询算法，是请求依次轮流分给后端服务器，一人一次，循环往复，不考虑服务器的负载和性能，即使故障也会发过去）；

添加两台后端 RS，指定 DR 模式 `-g`（-g 是 DR 模式，直接路由）；

最后可以通过 `ipvsadm -Ln` 核对规则；

**3. 在后端服务器 server2、server3（RS）上的服务器配置**

![](3.png) server3 和 server2 类似

**4. 在客户端（测试机）上验证一下**

![](4.png)
成功啦好的

但其实第一次实验时，实验失败了。分两种情况：

1）`curl 192.168.63.100` 行不通，返回超时。这时候我们可以通过抓包（`tcpdump -i ens32 host 192.168.63.100 and port 80`），检查一下 TCP 三次握手是否成功，如果没有回 SYN+ACK 应答包，应该是 server2 和 server3 的 ARP 内核参数有问题；

2）响应时只返回 server3，不返回 server2，检查调度器 server1。最开始操作时，把 VIP `192.168.63.100/24` 绑定在物理网卡 ens33，而非 lo 回环网卡、掩码 /32。虽然后续清理修正，但局域网内 ARP 缓存会持续生效，短时干扰网络。

> 📌 **补充知识点：排错时可用工具**
> - `tcpdump -i ens33 host 192.168.63.100 and port 80` → 抓包看三次握手
> - `arp -a` → 查看 ARP 缓存，确认 VIP 对应的 MAC 是调度器的
> - `arp -d 192.168.63.100` → 清除指定 ARP 缓存条目
> - `ipvsadm -Ln --stats` → 查看流量统计，确认请求是否到达 RS

**5. 一些疑问**

1）我们什么时候用 /24, ens33，什么时候用 /32, lo 取决于我们的使用角色。

Director 调度器使用 ens33，ens33 时对外的物理网卡，需要让全网用户访问到 VIP。而 RealServer 后端服务器使用 `VIP/32 dev lo`，绑定在 lo 回环网卡，只让本机内部程序识别 VIP，禁止对外响应 VIP 的 ARP 请求，防止冲突。

2）`arp_ignore` 和 `arp_announce` 是什么

- `arp_ignore`（收到别人 ARP 请求时，怎么应答）
  - 外部 ARP 询问 192.168.63.100 的 MAC 地址。请求包从 ens33 进来，但是 VIP 绑在 lo 上。此时 `arp_ignore=1` → 拒绝回复 ARP，防止 RS 对外宣称 VIP。
- `arp_announce`（本机对外发送 ARP 包时，源 IP 怎么选）
  - `arp_announce=2` 尽量使用和出接口网段匹配的 IP 作为 ARP 源地址。不会拿 lo 上的 VIP 当做源 IP 向外发送 ARP。能够防止 RS 主动向外广播【源 IP=VIP】的 ARP 报文。

> 📌 **补充知识点：arp_ignore / arp_announce 取值含义**
> | 参数 | 取值 | 含义 |
> |------|------|------|
> | arp_ignore | 0 | 只要本机有这个 IP，任何网卡都应答 |
> | arp_ignore | 1 | 只在接收请求的网卡上有该 IP 时才应答 |
> | arp_ignore | 2 | 只应答本机所有网卡上配置的 IP（更严格） |
> | arp_announce | 0 | 允许使用任意 IP 作为 ARP 源地址 |
> | arp_announce | 1 | 优先选择与目标 IP 同网段的地址 |
> | arp_announce | 2 | 始终选择最匹配的地址（最严格，DR 模式推荐） |

---

## 三、LVS-NAT 模式负载均衡（双网卡拓扑）

### （1）原理

1. VS/NAT 的优点是服务器可以运行任何支持 TCP/IP 的操作系统，它只需要一个 IP 地址配置在调度器上，服务器组可以用私有的 IP 地址。

2. 缺点是它的伸缩能力有限，当服务器结点数目升到 20 时，调度器本身有可能成为系统的新瓶颈，因为在 VS/NAT 中请求和响应报文都需要通过负载调度器。

3. 在这次实验里，调度器 VS 有双网卡：
   - **ens32（外网）**：192.168.10.100 【VIP，客户端访问入口】
   - **ens33（内网）**：192.168.63.131 【连接后端 RS，同时作为 RS 的默认网关】

   后端 RS 的网关仍在 192.168.63.x；测试的客户端在 192.168.10.x（与调度器外网同网段）

4. 工作原理是：

   客户端访问虚拟 VIP，请求报文到达调度器；调度器将目标 IP 修改为后端真实服务器 RIP 并转发（**DNAT**）。后端服务器收到请求后，响应报文必须经由调度器回包，调度器再把源 IP 改回 VIP（**SNAT**）返回客户端。入站、出站所有流量都经过调度器完成地址转换，从而实现四层负载均衡。

> 📌 **补充知识点：iptables 优先级 > IPVS**
> 如果调度器上 iptables 的 INPUT 链拦截了 80 端口，请求到不了 LVS，负载均衡就会失效。排查时先 `iptables -L -n` 确认没有 DROP 规则，或干脆 `systemctl stop firewalld` 关闭防火墙再测试。

### （2）具体的上机实验

1. 这个实验是 LVS 的另一种转发方式，用到了双网卡，调度器做网关。与 DR 模式不同，它不用处理 ARP，性能会弱一点。我们可以对比着来看。因为 NAT 和 DR 不能混用，我们要先清理上一轮 DR 实验的配置。

2. 在 server1 调度器上，我们要配置双网卡。于是我们关机后，返回 VMware，在设置里添加网络适配器，选择和现有网卡一致的网络连接模式连接（此处截图省略）。

   开机后，重新配置环境。

   ![](5.png)

   我们开启 IP 转发并让它生效，加载 net 模块，清空旧规则后，写入新的 LVS NAT 规则。

   > 📌 **关键命令补充**：
   > ```bash
   > # 开启内核路由转发（NAT 必须！否则 RS 回包过不来）
   > echo "net.ipv4.ip_forward = 1" > /etc/sysctl.d/lvs.conf
   > sysctl -p /etc/sysctl.d/lvs.conf
   >
   > # 加载 NAT 内核模块
   > modprobe iptable_nat
   > lsmod | grep nat
   >
   > # 写入 NAT 规则（-m = Masquerade 伪装模式）
   > ipvsadm -A -t 192.168.10.100:80 -s rr
   > ipvsadm -a -t 192.168.10.100:80 -r 192.168.63.128:80 -m
   > ipvsadm -a -t 192.168.10.100:80 -r 192.168.63.129:80 -m
   > ```
   >
   > 验证：`ipvsadm -Ln` 看到 Forward 列显示 **Masq** 就说明 NAT 模式配置正确。

3. 在 server2 和 server3 上设置新的网关，指向调度器的内网 IP。

   ![](6.png)

   > 补充完整命令：
   > ```bash
   > # 清理 DR 实验残留
   > ip addr del 192.168.63.100/32 dev lo
   > echo 0 > /proc/sys/net/ipv4/conf/lo/arp_ignore
   > echo 0 > /proc/sys/net/ipv4/conf/lo/arp_announce
   > echo 0 > /proc/sys/net/ipv4/conf/ens33/arp_ignore
   > echo 0 > /proc/sys/net/ipv4/conf/ens33/arp_announce
   > arptables -F
   >
   > # 修改网关指向调度器
   > ip route del default via 192.168.63.2
   > ip route add default via 192.168.63.131
   >
   > # 关闭防火墙和 SELinux
   > systemctl stop firewalld
   > setenforce 0
   > ```

4. 测试一下

   我们去 VMware 上，把 server4 的 IP 设置为静态的 192.168.10. 网段（才能 ping 通外网卡），然后通过 `curl 192.168.10.100` 测试一下。成功了。

   ![](7.png)

   > 📌 **补充验证**：NAT 模式下客户端 `arp -a` 看到的 VIP MAC 是调度器外网网卡 ens32 的 MAC，和 DR 模式一样，流量确实经过调度器。

---

## 四、Keepalived + LVS-DR 高可用集群（主备调度器，解决调度器单点故障）

### （1）原理

1. 只用 ipvsadm 部署 LVS-DR 存在两大缺陷：一个是调度器单点故障（只有一台 server1 做调度器，一旦宕机，整个集群全部断网）；另一个是无后端健康检查（后端 server2/server3 的 httpd 宕机，LVS 依旧分发流量，用户访问报错）。

2. Keepalived 是为 LVS 设计的高可用软件，基于 VRRP 虚拟路由冗余协议。

3. 核心原理是：VRRP 协议与 VIP 漂移

   Keepalived 的高可用能力依赖 VRRP 协议，本质是让多台节点（主、备）共享一个"虚拟 IP（VIP）"，对外呈现为一个统一的服务入口，核心逻辑如下：

   1）**角色划分**：集群包含 1 个主节点（Master）和 1 个/多个备节点（Backup）。
   2）**心跳检测**：主节点定期向备节点发送 VRRP 通告报文（默认每 1 秒），证明自己"存活"。
   3）**VIP 持有**：正常情况下，主节点绑定 VIP 并对外提供服务，备节点仅监听心跳，不持有 VIP。
   4）**故障切换**：若备节点超过指定时间（默认 3 秒）未收到主节点心跳，判定主节点故障，备节点立即升级为新主节点，绑定 VIP 并接管服务，整个过程无需人工干预。

4. 配置文件（以双节点为例）

   Keepalived 主配置文件默认路径为 `/etc/keepalived/keepalived.conf`，主、备节点配置差异主要在 `state`（角色）和 `priority`（优先级，数值越高越优先成为主节点）。

### （2）具体的实验如下

1. 实验拓扑（我的在 63 网段）

| 主机 | 角色 |
|------|------|
| server1 | Master LVS 调度器（192.168.63.131） |
| server4 | Backup LVS 调度器（192.168.63.130） |
| server2 | RS1（192.168.63.128）httpd 服务 |
| server3 | RS2（192.168.63.129）httpd 服务 |
| server4 | 同时作为备调度器 + 测试客户端 |

2. Master 调度器 server1（192.168.63.131）

   我们关闭全部的防火墙，保持 DR 环境，即在 server2、server3 上如下；

   ![](8.png)

   然后就回到 server1，清理旧的手动 LVS 规则、旧 VIP，安装 keepalived，编辑配置文件 `vim /etc/keepalived/keepalived.conf`，再启动服务；

   验证状态发现，ens33 上存在 192.168.63.100，也自动生成两条 RS 规则，Forward 为 Route（DR 模式）

   ![](9.png)

   具体的文件内容如下（server4 上也差不多）：

   ```keepalived
   ! Configuration File for keepalived

   global_defs {
       notification_email {
           root@localhost        # 接收警报的 email 地址,可以添加多个
       }
       notification_email_from keepalived@localhost  # 设置邮件的发送地址
       smtp_server 127.0.0.1    # 设置 smtp server 地址
       smtp_connect_timeout 30   # 设置连接 smtp 服务器超时时间
       router_id LVS_DEVEL       # load balancer 的标识 ID,用于 email 警报
       vrrp_skip_check_adv_addr  # 跳过对 VRRP 通告报文中 adv_addr 字段的检查
       #vrrp_strict              # 关闭"严格模式"
       vrrp_garp_interval 0      # 设置发送 GARP 报文的间隔时间，0 表示禁用
       vrrp_gna_interval 0       # 设置发送邻居通告报文的间隔时间，0 表示禁用
   }

   vrrp_instance VI_1 {
       state MASTER              # 备机改为 BACKUP,此状态是由 priority 的值来决定的
       interface ens33           # HA 监测网络接口，根据实际网络接口填写
       virtual_router_id 51      # 主、备机的 virtual_router_id 必须相同,取值 0-255
       priority 100              # 主机的优先级,主机优先级一定要大于备机
       advert_int 1              # 主备之间的通告间隔秒数

       authentication {          # 主备切换时的验证
           auth_type PASS         # 设置验证类型，主要有 PASS 和 AH 两种
           auth_pass 1111         # 设置验证密码，8 个字符以内
       }

       virtual_ipaddress {
           192.168.63.100        # 设置虚拟 IP 地址，每行一个
       }
   }

   virtual_server 192.168.63.100 80 {  # 定义虚拟服务器
       delay_loop 3              # 每隔 3 秒查询 realserver 状态
       lb_algo rr                # lvs 调度算法,这里使用轮询
       lb_kind DR                # LVS 使用 DR 模式，需要大写
       #persistence_timeout 50   # 会话保持时间（秒），无状态服务注释掉
       protocol TCP              # 指定转发协议类型

       real_server 192.168.63.128 80 {  # 配置服务节点
           weight 1              # 权值
           TCP_CHECK {           # realserver 状态检测
               connect_timeout 3 # 连接超时
               retry 3           # 重试次数
               delay_before_retry 3 # 重试间隔
           }
       }

       real_server 192.168.63.129 80 {
           weight 1
           TCP_CHECK {
               connect_timeout 3
               retry 3
               delay_before_retry 3
           }
       }
   }
   ```

   > 📌 **补充知识点：几个容易踩的坑**
   > - `vrrp_strict` 必须注释掉！否则 Keepalived 会自动添加 iptables 规则拦截流量，导致 curl 超时。
   > - `persistence_timeout` 无状态服务要注释掉，否则同一个客户端始终被分到同一台 RS，看不出轮询效果。
   > - `advert_int 1` 表示每秒发一次心跳，3 秒收不到就切换。
   > - `virtual_router_id` 主备必须相同，否则 VRRP 不通。

3. 后端 RS 健康检测（Keepalived 内置功能）

   我们通过 `systemctl stop httpd` 在 server2 模拟 web 服务故障，发现调度器上 server2 消失了，server2 恢复服务后，server2 自动重新加入集群，恢复负载分发。

   ![](10.png)

   ![](11.png)

   > 📌 **补充知识点：LVS 原生没有健康检测能力**
   > 只用 `ipvsadm` 时，后端 RS 宕机了 LVS 照样分发流量，用户会访问报错。Keepalived 的 `TCP_CHECK` 解决了这个问题——它每隔 `delay_loop` 秒去探测 RS 的 80 端口，连不上就自动摘除，恢复后自动加回。

